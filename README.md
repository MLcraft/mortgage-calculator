## Backend API code is found in `mortgage-calculator-api` folder
## Frontend UI code is found in `mortgage-calculator-web` folder

## After installing and running both the frontend and backend, visit `http://localhost:3000` to use.